<script>
    // Inicializa dataLayer si no existe
    window.dataLayer = window.dataLayer || [];
  
    // Define la función gtag
    function gtag(...args: any[]) {
      window.dataLayer.push(args);
    }
  
    // Configura Google Analytics
    gtag('js', new Date());
    gtag('config', 'G-NX1Z8DP4TX');
  </script>